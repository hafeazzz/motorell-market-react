# Auto detect text files and perform LF normalization
* text=auto

# 3D model binaries — jangan pernah di-normalisasi LF (bisa merusak file)
*.glb binary
*.gltf binary

# Aset gambar biner — jangan pernah dinormalisasi LF
*.png binary
*.ico binary
